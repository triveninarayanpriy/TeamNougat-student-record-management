plugins {
    id("com.android.application")
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id("dev.flutter.flutter-gradle-plugin")
}

android {
    // 1. CHANGED TO MATCH YOUR FIREBASE APP REGISTRATION
    namespace = "com.example.student_record_app"
    compileSdk = flutter.compileSdkVersion
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    defaultConfig {
        // 2. CHANGED TO MATCH YOUR FIREBASE APP REGISTRATION
        applicationId = "com.example.student_record_app"
        
        // 3. CHANGED FROM flutter.minSdkVersion TO 23 FOR FIREBASE
        minSdk = flutter.minSdkVersion
        targetSdk = flutter.targetSdkVersion
        versionCode = flutter.versionCode
        versionName = flutter.versionName
    }

    buildTypes {
        release {
            // Signing with the debug keys for now, so `flutter run --release` works.
            signingConfig = signingConfigs.getByName("debug")
        }
    }
}

kotlin {
    compilerOptions {
        jvmTarget = org.jetbrains.kotlin.gradle.dsl.JvmTarget.JVM_17
    }
}

flutter {
    source = "../.."
}

// 4. ADDED TO INITIALIZE FIREBASE PLUGINS (MUST BE THE VERY LAST LINE)
apply(plugin = "com.google.gms.google-services")
